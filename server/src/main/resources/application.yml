server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/swiftgram
    username: swift
    password: swiftpass
  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false

management:
  endpoints:
    web:
      exposure:
        include: health,info
